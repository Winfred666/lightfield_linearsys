data:
  points_dir: "data/points/20um_imgs/ds0p5_30000"
  
  # New RAW Mode
  # raw_A_dir: "data/raw/lightsheet_vol_6.9"
  # raw_b_dir: "data/raw/20um_imgs"
  # downsampling_rate: 0.5
  # scale_factor: 8.0 # Optional, default 8.0
  # max_pairs: null # Limit input pairs for testing
  # file_batch_size: 30000 # Points per batch file (cache)

  output_dir: "result/solve_point/newton/20um_imgs/ds0p5_30000"
  batch_pattern: "points_batch_*.pt" # Used if points_dir is active, ignored in raw mode (internal convention)
  threshold_A: 0.0001
  threshold_b: null

solver:
  type: "newton" # Options: newton, ista
  lambda_reg: 1.0e-6
  n_iter: 35
  positivity: true
  batch_size: 5000 # Sub-batch size for solving on GPU


device: "cuda" # or cpu

# Optional: export mesh after reconstruction.
# If omitted, no mesh is exported.
mesh_iso: [2.0, 4.0, 6.0]